{
  "name": "fittrack-backend",
  "version": "1.0.0",
  "description": "Cloudflare Workers API with D1 and R2 for FitTrack",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply fittrack-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply fittrack-db --remote"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.131.2"
  }
}
